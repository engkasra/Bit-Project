apiVersion: v1
kind: Service
metadata:
  name: django-service
  namespace: crypto-exchange
spec:
  selector:
    app: django-app
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
